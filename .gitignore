node_modules/
.astro/
.DS_Store

# Lokale Secrets (Smoobu) – niemals committen
.villa-secrets.env
villa-secrets.env

# SMTP-Zugangsdaten fürs Kontaktformular – nie committen (Repo ist
# öffentlich). Wird vom Deploy-Workflow aus GitHub-Secrets erzeugt und
# direkt auf den Webspace hochgeladen, siehe public/smtp-config.example.php.
public/smtp-config.php
smtp-config.php

# Geheime Umgebungsvariablen (z. B. SMOOBU_API_KEY) – nie committen
.env
.env.*

# Roh-Mediathek (nur Quelle für den Foto-Import; die einsortierten
# Galeriebilder unter src/images/ werden versioniert)
_originale/
